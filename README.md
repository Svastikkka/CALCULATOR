# CALCULATOR

SIMPLE CLI CALCULATOR

# Makefile

### Build project

Run: `make build`

Equivalent to: `gcc -Wall -Wextra src/main.c -o calculator -lm`


### Run project

Run: `make run`
or
Run: `echo "1+1" | ./calculator`


### Clean binary

Run: `make clean`